name: DoD Gates (Hybrid)

on:
  pull_request:
    branches: [ main ]

jobs:
  blocking-gates:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Implement blocking checks: tests, security, story linkage, session evidence"

  advisory-gates:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Implement advisory checks: refinement notes, sprint log freshness, metrics completeness"
