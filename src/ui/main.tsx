import React from 'react'
import ReactDOM from 'react-dom/client'
import NexusWorkbench from './NexusWorkbench'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NexusWorkbench />
  </React.StrictMode>,
)
